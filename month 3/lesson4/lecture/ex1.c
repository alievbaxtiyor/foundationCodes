#include <stdio.h>
#include <stdlib.h>

int main () {
    system("cls");

    char soz[] = {'B', 'a', 'x', 't', 'i', 'y', 'o', 'r'};
    int length = sizeof(soz);

    printf("%d", length);

    return 0;
}